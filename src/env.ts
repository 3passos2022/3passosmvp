
// Create environment variables file
// This prevents potential script loading issues

// Define any environment variables here
// If needed, you can access environment variables from import.meta.env
// Example: const apiUrl = import.meta.env.VITE_API_URL || 'default-url';

console.log("Environment variables loaded successfully");
